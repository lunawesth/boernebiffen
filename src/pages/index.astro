---
import MainLayout from "../layouts/MainLayout.astro";
import PasswordGate from "../components/PasswordGate.astro";
---

<MainLayout title="Forside" hideHeaderFooter={true}>
  <PasswordGate />
  <header
    class="absolute top-0 left-0 w-full z-20 p-2 md:p-6 flex flex-row justify-between md:justify-center md:items-center"
  >
    <div class="md:flex md:items-center pt-8.25">
      <img src="boernebiffen-logo.png" alt="Logo" class="w-40 md:w-70 h-auto" />
    </div>
    <div class="flex flex-row gap-10 md:absolute md:right-6 pt-6">
      <div class="my-auto">
        <img
          src="Search.png"
          alt="Søg"
          class="w-8 h-8 md:w-9 md:h-9 cursor-pointer"
          id="search-icon"
        />
        <input
          type="text"
          id="search-field"
          placeholder="Søg..."
          class="hidden border"
        />
      </div>
      <button
        class="bg-orange flex place-self-center rounded-full py-2 px-4 knap-logind cursor-pointer"
        >Log ind</button
      >
    </div>
  </header>

  <section class="grid md:grid-cols-2">
    <div class="bg-[url(/footer-bg.png)] pt-25 md:pt-50 md:pb-60">
      <h1 class="h1-forside text-center px-20 pt-25 pb-15">
        Velkommen hos Børnebiffen. <br />Gå på opdagelse i vores mere end 400
        kortfilm for de yngste.
      </h1>
      <a href="listeview">
        <button
          class="knap-kortfilm bg-orange flex place-self-center rounded-full px-8 py-3 md:py-2 md:px-10 cursor-pointer transition-transform duration-600
         hover:-translate-y-1 gap-3"
          >Kortfilm <img src="/pil.svg" alt="Pil" class="md:w-8" /></button
        ></a
      >
    </div>
    <div
      class="bg-[url(/footer-bg.png)] md:py-40 md:bg-[url(/bg-md.png)] md:block md:order-first"
    >
      <div class="place-self-center w-50 pt-20 md:w-70">
        <img src="app-ikon.png" alt="app" class="cursor-pointer" />
      </div>
      <p class="p-forside text-center px-37 pt-5 pb-30">
        Har du set, at vi har en app, hvor du kan se alle vores kortfilm? <br
        />Du kan hente den helt gratis i både App Store og Google Play.
      </p>
    </div>
  </section>
</MainLayout>

<!-- JavaScript -->
<script>
  const searchIcon = document.getElementById("search-icon");
  const searchField = document.getElementById("search-field");

  searchIcon.addEventListener("click", () => {
    if (searchField.classList.contains("hidden")) {
      searchField.classList.remove("hidden");
    } else {
      searchField.classList.add("hidden");
    }
  });
</script>

<!-- CSS -->
<style>
  .hidden {
    display: none;
  }
</style>
